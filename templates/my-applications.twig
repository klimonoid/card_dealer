{% extends 'parts/base.twig' %}

{% block body %}
    <h1>Мои заявления</h1>
    <p>На этой странице вы можете создать новое заявление на получении карты,
    а также посмотреть статус уже созданных</p>
    <div class="row mb-3">
        <div class="d-flex justify-content-end">
            <a class="btn btn-primary" href="/create-application">Создать заявление</a>
        </div>
    </div>
    <div id="applications-list">
        {% for application in applications %}
            <div class="card mb-3">
                <div class="card-body">
                    <h5 class="card-title">{{ application.id }}</h5>
                    <p class="card-text">
                        <b>Дата создания:</b> {{ application.date_of_submission }}
                        <br>
                        <b>Статус:</b> {{ application.status }}
                        <br>
                        <b>Комментарий:</b> {{ application.comment }}
                    </p>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}